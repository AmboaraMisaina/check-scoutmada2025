{% extends 'base.html.twig' %}

{% block title %}RegistrationFollowup{% endblock %}

{% block body %}
    <h1>RegistrationFollowup</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ registration_followup.id }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ registration_followup.status }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ registration_followup.createdAt ? registration_followup.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_registration_followup_index') }}">back to list</a>

    <a href="{{ path('app_registration_followup_edit', {'id': registration_followup.id}) }}">edit</a>

    {{ include('registration_followup/_delete_form.html.twig') }}
{% endblock %}
