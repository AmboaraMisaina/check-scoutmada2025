<form method="post" action="{{ path('app_badge_field_delete', {'id': badge_field.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ badge_field.id) }}">
    <button class="btn">Delete</button>
</form>
