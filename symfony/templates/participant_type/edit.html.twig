{% extends 'base.html.twig' %}

{% block title %}Edit ParticipantType{% endblock %}

{% block body %}
    <h1>Edit ParticipantType</h1>

    {{ include('participant_type/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_participant_type_index') }}">back to list</a>

    {{ include('participant_type/_delete_form.html.twig') }}
{% endblock %}
